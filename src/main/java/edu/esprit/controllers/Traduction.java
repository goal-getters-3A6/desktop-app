package edu.esprit.controllers;
/*import com.google.cloud.translate.Translate;
import com.google.cloud.translate.TranslateOptions;
import com.google.cloud.translate.Translation;*/
public class Traduction {
   /* private static final String API_KEY = "VOTRE_CLE_API"; // Remplacez par votre clé API

    public static String translate(String text, String sourceLanguage, String targetLanguage) {
        Translate translate = TranslateOptions.newBuilder().setApiKey(API_KEY).build().getService();

        Translation translation = translate.translate(
                text,
                Translate.TranslateOption.sourceLanguage(sourceLanguage),
                Translate.TranslateOption.targetLanguage(targetLanguage)
        );

        return translation.getTranslatedText();
    }

    public static void main(String[] args) {
        String textToTranslate = "Hello!";
        String translatedText = translate(textToTranslate, "en", "ro");
        System.out.println(translatedText); // Affichera "Bună ziua!" si tout fonctionne correctement
    }*/
}
